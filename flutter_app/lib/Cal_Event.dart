class Cal_Event {
  String name;
  String description;
  DateTime startTime;
  DateTime endTime;
  String repeat;

  Cal_Event(this.name, this.description, this.startTime, this.endTime, this.repeat);
}