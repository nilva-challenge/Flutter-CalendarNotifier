class Event {
  final String name;
  final String description;
  final DateTime startDate;
  final DateTime endDate;
  final String repeat;

  Event(this.name, this.description, this.startDate, this.endDate, this.repeat);
}
